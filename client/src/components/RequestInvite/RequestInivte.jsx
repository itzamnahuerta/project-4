import React, { Component } from 'react'
// want to learn how i came make this a request to sign up before giving the user access to the form

export default class RequestInivte extends Component {
    render() {
        return (
            <div className="request-invite">
                <div id="request-invite-text"> request invite </div>
            </div>
        )
    }
}
